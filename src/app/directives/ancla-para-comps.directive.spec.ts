import { AnclaParaCompsDirective } from './ancla-para-comps.directive';

describe('AnclaParaCompsDirective', () => {
  it('should create an instance', () => {
    const directive = new AnclaParaCompsDirective();
    expect(directive).toBeTruthy();
  });
});
